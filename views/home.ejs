<%- include("partials/header"); -%>

    <h1>Home</h1>

    <p> <%= startingContent %> </p>

    <p>
      <% for (var i=0; i<posts.length; i++) { %>
      <%   console.log("forloop" + posts[i].title); %>
    </p>
    <% }; %>

    <p>
      <% posts.forEach(function(post) { %>
      <%  console.log("forEach" + post.title); %>
      <% }); %>
    </p>


<%- include("partials/footer"); -%>
